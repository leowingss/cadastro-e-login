<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro de Usuários</title>
    <link href="/stylesheets/registro.css" rel="stylesheet">


</head>

<body>
    <h1>Cadastro</h1>
    <form action="/usuarios/criar" method="POST" class="formulario">
        <div class="container">
            <h3>Nome</h3>
            <input type="text" name="nome" id="" placeholder="Digite seu nome">
            <% if(locals.errors && errors.nome)  { %>
                <p class="msg-erro">
                    <%= errors.nome.msg %>
                </p>
                <%} %>
                    <h3>Email</h3>
                    <input type="email" name="email" id="" placeholder="Digite seu email">
                    <% if(locals.errors && errors.email)  { %>
                        <p class="msg-erro">
                            <%= errors.email.msg %>
                        </p>
                        <%} %>
                            <h3>Senha</h3>
                            <input type="password" name="senha" id="" placeholder="Digite sua senha">
                            <% if(locals.errors && errors.senha)  { %>
                                <p class="msg-erro">
                                    <%= errors.senha.msg %>
                                </p>
                                <%} %>
                                    <button type="submit">Enviar</button>
        </div>
    </form>

</body>

</html>